# majestic-canon-tools — Publish to R2 CDN
# Copy to .env and fill in values. Do not commit .env.

# R2 (Cloudflare) — required for pnpm publish:cdn
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_CDN_BASE_URL=https://cdn.majesticcore.dev/canon

# R2 — optional (default: majestic-canon-dist)
# R2_BUCKET_NAME=majestic-canon-dist

# Build paths — optional
# MAJESTIC_CANON_PATH=../majestic-canon
# MAJESTIC_PACK_PATH=./out

# Signing — optional; used for verify step
# MAJESTIC_SIGNING_PUBLIC_KEY=./out/test-signing.pub
